# Аудит — Фаза 2: знахідки та виправлення

> Живий журнал. Кожен рядок = одна проблема. Одна проблема = один коміт.
> Класифікація: **CRITICAL** (пряма втрата/крадіжка коштів або обхід гейту) ·
> **MAJOR** (крихкість/незахищений інваріант, що за реальних умов веде до
> втрати або некоректного стану) · **MINOR** (якість, без грошового ризику) ·
> **NITPICK** (стиль). MINOR/NITPICK — тільки репорт, без патча (за правилами).

Порядок модулів (за домовленістю): **store → settlement → payment → identity
→ reputation**. Пріоритет ваги: цілісність коштів → безпека → решта.

---

## Зведена таблиця

| # | Модуль | Severity | Стан | Коміт | Тест (fail→pass) |
|---|---|---|---|---|---|
| F1 | config/payment/agent_client (float→wei) | MAJOR | **виправлено** (wire = B3) | `09b3a4c` | `test_money_scaling.py` ✓ |
| F2 | store.py (single-process) | MAJOR | **виправлено** | `51e4b17` | `test_store_single_process.py` ✓ |
| F3 | settlement.py (partial failure) | MAJOR | **виправлено** | `1530c73` | `test_settlement_partial_failure.py` ✓ |
| F4 | server `/registry/register` + capability.resolve | **CRITICAL** | **виправлено** (full #A: підписана реєстрація + payTo-binding + агент-check) | `2a08941` | `test_registry_signed.py` ✓ |
| F5 | agent_client `SpendLedger` / server `_ledger` (float) | MAJOR | **виправлено** (у складі F1) | `09b3a4c` | `test_money_scaling.py` ✓ |
| F6 | payment.py overpayment `value > price` | MAJOR | **виправлено** | `1ec1e9e` | `test_payment_overpayment.py` ✓ |
| F7 | agent_stats sybil (self-dealing накручує completed) | MAJOR? | **свідома межа, задокументовано** → `03-reputation-threat-model.md` | — | — |
| F8 | identity `reputation_score` cold-start proxy | MINOR | report-only | — | — |
| F9 | store: стара схема .db падала пізно й криптично | MAJOR | **виправлено** (PRAGMA user_version guard + README) | `36ded73` | `test_store_schema_version.py` ✓ |

---

## F1 — Гроші як float на межі конверсії (MAJOR)

**Факт (перевірено, не припущення).** Ціни в конфізі — `float`
(`config.py:80-81,89`), конвертуються у wei через `round(float(price)*1e6)`
(`payment.py:117`, `agent_client.py:160`). Офчейн-облік накопичується у float:
`SpendLedger.spent_teurc += ...` (`agent_client.py:124`),
`_ledger["earned_teurc"] += ...` (`service_provider/server.py:241`). Поля
відповіді `amount/fee/net_..._teurc` — float (`whitechain_facilitator.py:171-173`).

**Емпірика (замість одного прикладу).** `tests/test_price_scaling_audit.py`
детерміновано порівнює `round(float(price)*1e6)` з точним `Decimal(price)*1e6`:
на 250k+ значеннях (вичерпно 0..0.05 + 200k випадкових + сконфігуровані) —
**нуль розходжень для цін з ≤6 знаками**. Тобто на легальних цінах поточний
код НЕ мисцінує. Розходження з'являється лише для цін з **>6 знаками**
(суб-wei), які код **мовчки округлює** замість відхиляти.

**Чому MAJOR, а не CRITICAL.** Немає розходження на жодній представній ціні →
це не активна втрата коштів. Але: (1) float-накопичувачі дрейфують на багатьох
операціях (spend-cap-гейт і цифра заробітку); (2) ціна з >6 знаками
приймається тихо. Клас знімається рішенням №4 (Decimal→int на завантаженні,
int wei всюди в офчейн-обліку, відхилення цін >6 знаків).

**Блокер:** внутрішній перехід на int wei безпечний, але **представлення на
дроті** (`*_teurc` float у відповіді facilitator-а + REAL-колонка
`capabilities.price`) — публічний контракт. Варіанти — у
`02-open-questions.md #B`. **Стоп до рішення автора.**

---

## F2 — «Один процес» був незахищеним інваріантом (MAJOR) — ВИПРАВЛЕНО

**Факт.** `threading.Lock` у `store.py` серіалізує записи лише в межах одного
процесу. Запуск кількох воркерів (`uvicorn --workers N`) на тому самому файлі
БД дав би кожному свій Lock → втрата атомарності лічильників.

**Виправлення (`51e4b17`, рішення автора №1).** Ексклюзивний неблокуючий
`fcntl.flock` на сайдкар `<db>.lock` на старті: другий процес/інстанс падає зі
`StoreLockError` і зрозумілою помилкою. Межу задокументовано коментарем у
`store.py` (Lock — тільки в процесі; масштабування потребує транзакцій рівня
БД; БД свідомо не мігровано). `:memory:` звільнено (окрема БД на з'єднання).

**Тест:** `tests/test_store_single_process.py` — падав до патча (немає
`StoreLockError`), проходить після (3 кейси: другий Store падає; reopen після
close(); memory без локу).

---

## F3 — Частковий збій сеттлменту без журналу і реконсиляції (MAJOR) — заплановано M2

**Факт.** `settle()` робить дві окремі транзакції: релей
`transferWithAuthorization` (кошти → facilitator, `settlement.py:72-86`) і
форвард нетто сервісу (`settlement.py:100-106`). Якщо релей замайнено, а
форвард ревертнув (`settlement.py:111-112`) — платник списаний, кошти застрягли
у facilitator, контенту немає, **у store немає запису для звірки**.

**План (рішення автора №3, БЕЗ авто-ретраю).** Журнал у store ДО бродкасту;
при реверті форварду — явний стан «кошти утримані, зобов'язання не виконане»
з можливістю звірки. Мовчазна втрата неприйнятна. Тест на сценарій
обов'язковий (покриє непокриті `settlement.py:92,112`).

---

## F4 — `/registry/register` без авторизації + керований вибір провайдера (CRITICAL) — СТОП

**Факт.** `POST /registry/register` не має авторизації (`server.py:130-145`).
`resolve()` повертає `matches[0]` (`capability.py:46`), де `matches`
відсортовано `ORDER BY id` (`store.py:173`), а `id` — рядок, що його **задає
реєстрант** (`capability.py:29`). Тобто вибір детермінований, але **керований**:
атакер реєструє запис того ж `capability_type` з малим `id` (напр. `"0"`) →
сортується першим → `resolve()` віддає його `provider_url`. Агент іде на сервер
атакера, той повертає 402 зі своїм `payTo`, і агент підписує авторизацію на
**чужу адресу** (`agent_client.py:226-238` бере `payTo` з 402 без перевірки).
Наслідок: перенаправлення коштів.

**Це зміна публічного API** (підпис реєстрації + прив'язка payTo + контракт
resolve). Мінімальний дизайн — у `02-open-questions.md #A`. **Стоп до рішення.**

---

## F5 — float-накопичувачі офчейн (MAJOR) — входить у F1

`SpendLedger.spent_teurc` і `_ledger["earned_teurc"]` — float `+=`. Не
авторитетна сума, але spend-cap-гейт і цифра заробітку. Переходить на int wei
разом з F1 (рішення №4).

---

## F6 — Overpayment `value > price` приймається мовчки (MAJOR) — заплановано M3

`payment.py:118` перевіряє лише `value >= price_wei`. `value > price`
приймається і сеттлиться повністю (`settlement.py` бере повний `value`). За
рішенням №5 мовчазне прийняття неприйнятне. Рекомендація і фікс — у M3
(payment.py), обґрунтування в тому ж комі.

---

## F7 — Sybil/self-dealing репутації (MAJOR?, report-only — потребує підтвердження автора)

`agent_stats.completed_payments` інкрементується локально на кожному успішному
розрахунку (`whitechain_facilitator._record_completed_payment`). Агент, що
контролює і платника, і сервіс, може ганяти платежі сам собі й накручувати
`completed` → піднімати behavioral tier (F-формула, 30·completed_norm). KYA-гейт
(WB Soul) частково стримує (потрібен верифікований Soul), а SBT-attested tier —
он-чейн і не накручується локально; але поведінкова складова — так.

**Чому не патчу:** це рішення моделі загроз, а не однозначний баг. Варіанти
(anti-sybil: враховувати лише платежі до РІЗНИХ контрагентів; вимога стейку;
он-чейн-джерело статистики) — це зміна дизайну репутації, яку автор має
підтвердити. За правилами — **report-only**. Джерело: `whitechain_facilitator.py`
`_record_completed_payment`, `reputation.py`.

## F8 — cold-start `reputation_score` як проксі (MINOR, report-only)

`identity.py:91` для агента без історії ставить `score = attested_tier * 35`
(0/35/70) — грубий проксі ЛИШЕ для показу. Гейт цим не керується (гейт — на
`reputation_tier = max(attested, behavioral)`), і код це коментує. Розходження
score↔формула суто косметичне. Не патчу (MINOR + report-only).

## Огляд identity.py / reputation.py (модулі 4–5, пріоритет «решта»)

Обидва — чисті, single-responsibility, добре покриті (identity 97%,
reputation 100%). `reputation.py` використовує float, АЛЕ це 0..100 евристичний
**score**, не гроші — заборона «float для грошей» не застосовна. KYA/SBT
читаються он-чейн через єдиний seam (`IdentityReader`), підписи не перевіряються
свідомо (це робота `payment.py`). CRITICAL/MAJOR не знайдено; єдині зауваги —
F7 (sybil, крос-модульне) і F8 (косметика). Патчів у ці модулі не вносив.

---

## Самоперевірка (обов'язкові зони покриття)

| Зона | Тест | Стан |
|---|---|---|
| (A3) Часткові збої розрахунку | `test_settlement_partial_failure.py` (relay ok/forward revert → FundsHeld, звірка) | ✓ |
| (A4) Ідемпотентність / replay | `test_facilitator.py::test_nonce_replay_rejected` (on-chain nonce) | ✓ |
| (A5) Межові суми | `test_money_scaling.py` (0, суб-wei reject, точна межа ledger), `test_payment_overpayment.py` (від'ємна, under/over/exact) | ✓ |
| (B) Конкурентність | `test_store_concurrency.py` (8×200 інкрементів без втрат), `test_store_single_process.py` (мультипроцес заборонено) | ✓ |

Тести детерміновані: без мережі (крім локального eth-tester), без `sleep`, без
залежності від системного часу в асертах (час монотонно перевіряється лише через
явні validAfter/validBefore).

---

## Підсумковий вердикт

**Виправлено в цьому проході (кожне — окремий коміт + тест fail→pass):**

| # | Severity | Суть | Коміт |
|---|---|---|---|
| F1/F5 | MAJOR | float у грошових шляхах → int wei всюди (config/payment/ledger/wire B3) | `09b3a4c` |
| F2 | MAJOR | «один процес» форсовано файловим локом | `51e4b17` |
| F3 | MAJOR | частковий збій сеттлменту → журнал + стан «кошти утримані» + звірка (+ ендпойнт `b6526e5`) | `1530c73` |
| F4 | **CRITICAL** | реєстр: підписана реєстрація + payTo-binding + перевірка на боці агента (full #A) | `2a08941` |
| F6 | MAJOR | overpayment відхиляється; типізація входу авторизації | `1ec1e9e` |
| F9 | MAJOR | версіонування схеми store; стара .db відхиляється гучно на старті | `36ded73` |

**Свідомо НЕ патчено (задокументовано як межа):**
- **F7 (MAJOR?)** — sybil/self-dealing репутації. Явна модель загроз:
  `03-reputation-threat-model.md` (на чому тримається довіра, від чого НЕ
  захищає, що потрібно було б). Довірчий інваріант тримає **on-chain** шар
  (KYA + SBT); behavioral — зручність, не бар'єр.
- **F8 (MINOR)** — косметичний score-проксі, report-only.

**Покриття грошових/безпекових модулів:** payment 98%, settlement 100%,
store 100%. Повний набір: **93 passed**; наскрізне демо зелене. Solidity-
контракти в цьому проході не чіпалися.

**Що лишилось (roadmap, не блокери):**
1. **F7 sybil** — реалізувати захист (крос-контрагентність / on-chain
   провенанс / стейк), коли behavioral-репутація має гейтити самостійно.
2. Реальні dispute/refund-сигнали (зараз члени формули нейтральні).
3. Авто-реконсиляція утриманих коштів (F3 дає стан+звірку+ендпойнт; повернення
   — свідомо ручний крок).
4. Пін залежностей (lock-файл) — зараз лише `>=`.
5. Прибрати мертвий код (`author/agent.py`, `send_wbt`, deps redis/pillow) або
   лишити з поміткою.

---

## Стоп-пойнти

Обидві зміни публічного API/схеми, що вимагали узгодження (#B wire-формат,
#A реєстр), **узгоджено і реалізовано**. Відкритих стоп-пойнтів немає. Правило
на майбутнє (за вказівкою автора): якщо я сам виношу щось як питання — чекаю
відповіді, навіть якщо відповідь здається очевидною.
