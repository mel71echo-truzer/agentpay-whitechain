# Скріншоти для головного README

Поклади сюди два файли — головний `README.md` уже посилається на них:

- `terminal.png` — вивід `python run_demo.py "..."` у терміналі (весь цикл:
  запит → 402 → оплата → фото, і фінальний підсумок).
- `explorer.png` — сторінка адреси гаманця Фотобанку в Whitechain explorer
  (`https://testnet.whitechain.io/address/...`) з видимими транзакціями.
