# Додаткові залежності лише для розробки/тестів — НЕ потрібні для запуску scripts/demo.py
# проти реального Whitechain testnet.
# py-evm дає локальний EVM-ланцюжок в пам'яті — NETWORK=local (за замовчуванням)
# використовує його і для scripts/demo.py, і для tests/.
#
# ВАЖЛИВО (переносимість на Windows): НЕ використовуємо eth-tester[py-evm].
# Той extra тягне eth-hash[pysha3] -> safe-pysha3, у якого немає готових колес
# для Windows ЖОДНОЇ версії Python, тож pip намагається зібрати C-розширення
# і падає з "Microsoft Visual C++ 14.0 or greater is required".
# py-evm сам по собі pysha3 не потребує; eth-hash чудово працює на бекенді
# pycryptodome, який і так є в дереві залежностей (через eth-keyfile).
eth-tester
py-evm>=0.12.0b2,<0.13.0b1
eth-hash[pycryptodome]>=0.1.4,<1.0.0
pytest>=8.0.0
# Coverage (Phase 3): `python -m pytest --cov` (config in .coveragerc). See COVERAGE.md.
pytest-cov>=5.0.0
# Solidity static analysis (Phase 2). Run per docs/audit/06-slither.md; needs a
# solc 0.8.24 binary (scripts/bootstrap_solc.sh provides a sha256-verified one).
slither-analyzer>=0.11.0
